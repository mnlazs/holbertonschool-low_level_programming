#include <stdio.h>

/**
 * main -  number of arguments
 * @argv: character to print
 * @argv: character to print
 * Return: 0
 **/
int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc -1);
	return (0);
}
