#include "main.h"
#include <string.h>

/**
 * strlen - funtion prototype
 *
 * @str: to get the length of the string
 *
 * Return: the lenght of @str.
 */
int _strlen(char *s)
{
	size_t length = 0;

	while (*str++)
		length++;
	return (length);
}
